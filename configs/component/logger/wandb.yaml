_target_: iif.utils.pytorch_lightning.wandb.MyWandbLogger
offline: False
anonymous: null # enable anonymous logging
log_model: False # upload lightning ckpts
prefix: "" # a string to put at the beginning of metric keys
group: ""
tags: ${tags}
job_type: ""
sweep_job_id: ${hydra:job.id}

name: ${hydra:runtime.choices.task} # name of the run (normally generated by wandb.yaml)
save_dir: ${paths.log_dir}/${hydra:runtime.choices.task}
id: null # pass correct id to resume experiment!
project: "iif"
entity: "peter-kocsis"  # CHANGE TO YOUR ENTITY