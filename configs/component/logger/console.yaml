_target_: iif.component.logger.console.ConsoleLogger
name: null # name of the run (normally generated by wandb.yaml)
save_dir: "${paths.log_dir}/${hydra:runtime.choices.task}"
id: null # pass correct id to resume experiment!
project: "pir"
entity: "peter-kocsis"
plot_images: True
save_images: False
log_folder: null
save_HDR: False